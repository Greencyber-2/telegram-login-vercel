{
  "name": "telegram-login-vercel",
  "version": "1.0.0",
  "description": "Telegram login system for Vercel",
  "main": "api/login.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "telegram": "^2.13.1"
  },
  "engines": {
    "node": ">=16.x"
  }
}
